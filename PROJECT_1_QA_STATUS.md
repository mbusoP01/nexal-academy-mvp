# Project 1 QA Status

Local gates include JavaScript syntax, curriculum validation, internal-link
checking and static HTTP route smoke. Authenticated Supabase, RLS and live
progress writes require the configured external project and are reported
separately when unavailable.
